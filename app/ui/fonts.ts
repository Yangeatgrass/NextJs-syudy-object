import { Inter, Lusitana, Rubik_Bubbles } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: '400'
});

export const rubikBubbles = Rubik_Bubbles({
  subsets: ['latin'],
  weight: '400',
});